export const ColorChanger = ({ color_componente }) => {
  return (
    <div
      style={{
        backgroundColor: color_componente,
        width: "500px",
        height: "500px",
      }}
    ></div>
  );
};
